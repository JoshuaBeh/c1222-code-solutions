import React from 'react';
import ReactDOM from 'react-dom/client';
import AppDrawer from './app-drawer';

const conntainer = document.querySelector('#root');
const root = ReactDOM.createRoot(conntainer);
root.render(<AppDrawer />);
